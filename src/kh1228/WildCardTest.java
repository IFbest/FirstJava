package kh1228;

import java.util.List;

public class WildCardTest {
    public static void main(String[] args) {
     /*   double isum = sum(dList);
        //double 타입의 isum은 iList의 합계
        double dsum = sum(dList);
        //double 타입의 dsum은 dList의 합계
        //sum(sList); // 오류발생
        System.out.println("inum의 합계 : " + isum);
        System.out.println("dnum의 합계 : " + dsum);
    }
    public static double sum(List<? extends Number> list){//상한이 있는 와일드 카드
        //상위 제한 설정하여 수퍼클래스 또는 수퍼클래스를 상속받은 하위 객체만 타입으로 지정 가능
        //<? super 하위클래스/인터페이스> 하위제한 설정 extends와 반대개념이다 하한이 있는 와일드 카드
        //<-> 반대로<? super 서브클래스>는 하위 제한을 설정한 것.
        // 와일드카드에 하위 제한을 두면 서브클래스 또는 서브클래스가 상속하고 있는
        //상위 객체만 타입으로 지정 가능
        double total = 0;// double타입의 total을 0으로 초기화
        for (Number v : list) {
            total += v.doubleValue();
        }// Number타입의 v가 list인 경우 total은 doubleValue의 합과 같음
        return total;//total 값 반환함*/
    }
}
